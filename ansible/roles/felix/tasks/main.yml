---
- name: Felix (command-line file manager)
  become: true
  pacman:
    name: felix-rs
    state: present
- name: Install Chafa (image previewer for Felix)
  become: true
  pacman:
    name: chafa
    state: present
- name: Felix setup
  synchronize:
    src: felix
    dest: "/$HOME/.config"
